#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    while (t--)
    {
        int n, k;
        cin >> n >> k;
        auto check=[&](int m)
        {
            
        };
        long long l=2,r=5*10e17,mid,ans=LLONG_MAX;
        while(l<=r)
        {
            mid=l+(r-l)/2;
            if(check(mid))
            {
                ans=
            }
        }
    }
    return 0;
}
#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        vector<int> v(n);
        for (int i = 0; i < n; i++)
            cin >> v[i];
        int op = 0;
        for (int i = 0; i < n;)
        {
            int mx = 0;
            if (v[i] > 0)
            {
                int l = i;
                int mx = 0;
                while ((i%2==j%2))
            }
        }
    }
    return 0;
}
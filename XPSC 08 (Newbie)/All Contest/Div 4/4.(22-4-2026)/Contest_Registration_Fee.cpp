#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n,k;
    cin >> n >> k;
    if(n>=k) cout << 0 << endl;
    else cout << 100 << endl;
    return 0;
}
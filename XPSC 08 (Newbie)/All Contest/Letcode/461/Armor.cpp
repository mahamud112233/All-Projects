#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int a,d;
    cin >> a >> d;
    if(a<=d)
        cout << "Yes\n";
    else    cout << "No\n";
    return 0;
}